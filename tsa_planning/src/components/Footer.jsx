import React, { useEffect, useState } from "react";
import './Footer.scss';

export const Footer = () => {
    return (
        <footer className="footer">
            Some footer text &copy;
        </footer>
    );
}